import { AdddropdownDirective } from './adddropdown.directive';

describe('AdddropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new AdddropdownDirective();
    expect(directive).toBeTruthy();
  });
});
